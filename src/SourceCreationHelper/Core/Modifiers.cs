namespace SourceCreationHelper.Core
{
    public enum AccessModifier{
        @public,
        @internal,
        @private,
        @protected,
        @protected_internal,
        @private_protected
    }

}